services:
  api:
    image: ${API_IMAGE:-ghcr.io/renatograsso10/rinha-backend-2026-go-runtime:latest}
    build:
      context: .
      dockerfile: Dockerfile.runtime
    ports:
      - "9999:8080"
    environment:
      CLASSIFIER_MODE: ${CLASSIFIER_MODE:-forest}
      VISIT_CAP: ${VISIT_CAP:-256}
      GOMAXPROCS: "1"
      GOMEMLIMIT: 256MiB
      GOGC: "100"
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: "350MB"
