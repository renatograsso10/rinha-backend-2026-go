events {
    worker_connections 4096;
    multi_accept on;
}

stream {
    upstream api {
        server api1:8080;
        server api2:8080;
    }

    server {
        listen 9999 reuseport;
        proxy_pass api;
        proxy_connect_timeout 1s;
        proxy_timeout 5s;
    }
}
